{
  "name": "todo-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm --workspace backend run dev",
    "start": "npm --workspace backend run start"
  }
}
